apply plugin: "war"

bootWar {
    mainClass = "com.apratim.practice.JavaPracticeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
